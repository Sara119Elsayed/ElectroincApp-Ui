class Product {
  int id, price;
  String title, subtitle, image, description;
  Product({
    required this.id,
    required this.price,
    required this.title,
    required this.subtitle,
    required this.image,
    required this.description,
  });
}
